# 에러 재발방지 로그 — apt_lottery

> **같은 에러를 두 번 겪지 않기 위한 문서.**
> - 작업 시작 시 이 문서를 먼저 읽는다.
> - 에러를 해결할 때마다 아래 표에 한 줄 추가한다(증상·원인·해결·재발방지).
> - 재발방지 열에는 "다음엔 이렇게 하면 안 겪는다"를 적는다.
> 최초 생성: 2026-07-15

| 날짜 | 증상 | 원인 | 해결 | 재발방지 |
|------|------|------|------|----------|
| 2026-07-10 | 청약홈 데이터 요청이 계속 HTTP 401 | MCP 서버 래퍼로 붙이려 했으나 인증이 통과되지 않고, 날짜·유형 필터 파라미터도 노출되지 않음 | MCP를 버리고 공식 API(`api.odcloud.kr/api/ApplyhomeInfoDetailSvc/v1`)를 파이썬으로 직접 호출 | 공공데이터포털 API는 MCP 래퍼를 거치지 말고 직접 호출한다. 래퍼는 서버측 필터를 못 쓰는 경우가 많다 |
| 2026-07-10 | 인증키를 넣어도 401 — Encoding/Decoding 중 어느 키인지 불명 | data.go.kr이 인증키를 2종(Encoding/Decoding)으로 발급하는데, 엔드포인트가 기대하는 형태가 문서에 없음 | `fetch_subscriptions.py`가 4가지 인증 방식(serviceKey 쿼리 quote/raw → Authorization 헤더 raw/unquote)을 순서대로 시도하고 401이 아닌 첫 응답을 채택 | 키 형태를 추측하지 말고 폴백 체인으로 자동 탐색하게 짠다. 그러면 어느 키를 넣어도 동작한다 |
| 2026-07-15 | 자격진단 웹앱이 **이미 마감된 공고를 TOP 추천**으로 노출(배지도 `D--3`으로 깨짐). 27건 중 17건(63%)이 마감건 | ①매칭 조건 `dday<=7`이 음수를 걸러내지 않음 ②근본 원인: `dday`가 분석 시점(report_date)에 계산돼 **박제된 값** — 스냅샷이 오래될수록 계속 틀어짐(07-10 계산값을 07-15에 사용) | 브라우저에서 `apply_end` vs 오늘로 **D-day 재계산**, `dday<0`이면 추천에서 제외하고 제외 건수를 화면에 노출 | **날짜 파생값을 데이터에 박제하지 않는다.** 기준일이 바뀌면 틀어지는 값(D-day·마감여부)은 표시 시점에 계산한다. 스냅샷 데이터를 쓰는 화면은 반드시 "기준일"을 노출할 것 |
| 2026-07-15 | 강의 슬라이드 HTML의 한글이 전부 깨져서 표시(`泥�빟 �섎꽕��`) | `class_slide_apple/assets/template.html`에 `<meta charset>`이 없음. 파일은 UTF-8인데 브라우저가 인코딩을 추측 → file:// 및 charset 없는 서버에서 mojibake | 문서 최상단에 `<meta charset="utf-8">`(+viewport) 추가 | 자기완결 HTML을 낼 때 charset 메타를 **먼저** 넣는다. 템플릿에 없으므로 복사 직후 확인. 브라우저로 실제 렌더를 봐야 잡힌다 — 파일만 읽으면 안 보임 |
| 2026-07-10 | 주택명·마감일 필드가 유형마다 달라 파싱이 깨짐 | 엔드포인트별로 응답 필드명이 상이(`RCEPT_ENDDE` vs `SUBSCRPT_RCEPT_ENDDE` vs `GNRL_RNK*_ENDDE`), 날짜 포맷도 dash/plain 혼재 | collector는 원본을 가공 없이 저장(+`_house_type` 라벨만 부가)하고, 필드 해석은 analyst에 위임. 확정 매핑은 `subscription-analyze` 스킬에 문서화. 마감은 "마감 성격 날짜 중 가장 늦은 값"을 대표로 | 수집과 해석의 책임을 분리한다. 수집 단계에서 정규화하면 유형이 늘 때마다 수집기가 깨진다 |
| 2026-09-05 | 경쟁률 API `CMPET_RATE`가 숫자가 아닌 `"(△15)"`로 와서 float 변환 실패, `null`/`"-"`도 혼재 | 기술문서 명세에 없는 표기 — △는 **미달 세대수**(공급 15 중 15 미달), `-`/null은 접수 전·집계 전 | `analyze.py parse_rate()`가 정규식으로 △N→(0.0, short=N), `-`/null→미집계로 분기. `LWET_SCORE` 0도 커트라인 아님(추첨/미달 행)이라 제외 | 공공 API는 명세보다 **실응답을 먼저 몇 건 찍어보고** 파서를 짠다. 숫자 필드에 기호·괄호·콤마(`"62,342"`)가 섞여 오는 걸 기본 가정으로 |
| 2026-09-05 | 웹앱이 오피스텔 공고에 "신혼부부 특공(민영)" 경로를 붙여 추천 | 특공 경로 판정을 유형 구분 없이 모든 공고에 적용 — 오피스텔/생숙은 특공 자체가 없고, 공공임대는 신혼/청년/고령자만 | 유형별 분기: 오피스텔 = 통장·자격 무관 단일 경로, 공공임대 = `models[].special` 배정 있는 유형만, APT_일반만 §3 특공 매핑 | 자격 규칙은 **물건 유형별로 먼저 갈라놓고** 시작한다. "공고"라는 한 덩어리로 다루면 유형 고유 규칙이 새어 들어간다 |
| 2026-09-05 | 청약홈 원문 대조에서 매칭 규칙 오류 4건 발견: 다자녀 "3명↑"(실제 미성년 2명↑), 노부모 특공 세대주 요건 누락, 생애최초 소득세 5년 요건 누락, 배우자 통장 가점 "3년↑ 3점"(실제 2년↑ 3점) | 지식베이스가 2024 강의 기억에 의존했고 공식 페이지와 대조한 적이 없었음. 1순위 요건(가입기간·납입횟수·예치금·규제지역 강등)은 검사 자체가 없었음 | 청약홈 청약자격·특별공급·용어설명(40개) 원문을 스냅샷으로 저장(`subscription-match/references/청약홈-용어설명-원문.md`)하고 지식베이스·매칭 스킬·웹앱 JS 세 곳을 동시에 수정. 웹앱에 납입횟수·통장종류·거주지·예치금·소득세연수·5년내당첨 입력 추가 | **자격 규칙은 강의·기억이 아니라 공식 원문 스냅샷을 근거로 쓴다.** 규칙 파일 상단에 대조 일자를 적고, 규칙을 바꿀 땐 문서+스킬+JS 세 곳을 같이 고친다 |
| 2026-09-06 | 웹앱이 1주택자에게 무순위 공고를 "유주택이어도 대부분 지원 가능"이라며 추천 | 강의 기억("무순위=아무나")을 그대로 씀. 청약홈 잔여세대 안내 원문은 무순위·불법행위재공급 = **무주택세대구성원만**, 유주택 가능은 임의공급뿐 | 잔여세대 3종을 `house_secd`로 갈라 무순위는 유주택 제외·규제지역 재당첨제한 경고, 임의공급만 유주택 허용 | "자격 무관"이라는 말은 유형별 원문 표를 보기 전엔 쓰지 않는다. 같은 카테고리(잔여세대)라도 하위 유형마다 자격이 다르다 |

<!--
작성 예시:
| 2026-07-14 | 텔레그램 봇 HTTP 409 반복 | 같은 봇 토큰을 다른 곳에서도 getUpdates 폴링 중(봇당 폴러 1개만 허용) | 전용 봇 토큰 신규 발급 | 봇마다 토큰 분리. 발송(sendMessage)은 공유 무해, 수신(getUpdates)만 배타적임을 기억 |
| 2026-07-14 | 5천건 병합 2분 타임아웃 | 제목 유사도 O(n²) 전량 비교 | 정규화 제목 접두어 버킷팅 후 버킷 내 비교 | 근사 중복제거는 블로킹으로 후보를 먼저 좁힌다 |
-->
